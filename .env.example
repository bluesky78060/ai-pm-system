# SQLite database path (default: ./data/pm.db)
DB_PATH=./data/pm.db

# Express HTTP server port (default: 3100)
HTTP_PORT=3100

# GitHub integration (optional)
GITHUB_TOKEN=
GITHUB_REPO=

# Log level (default: info)
LOG_LEVEL=info
